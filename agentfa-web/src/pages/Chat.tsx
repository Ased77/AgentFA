import { FormEvent, useState } from "react";
import { Download, Paperclip, Send, Trash2, Wallet, X } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { agents } from "../data/agents";
import { canSend, getWallet, recordUsage, topUp, Wallet as WalletType } from "../lib/mock-store";
type Message={role:"assistant"|"user";text:string;time:string;tokens?:number};
const fa=(n:number)=>n.toLocaleString("fa-IR");
function Code({text}:{text:string}){return <>{text.split(/(```[\s\S]*?```)/g).map((part,i)=>part.startsWith("```")?<div className="code-block" key={i}><button onClick={()=>navigator.clipboard.writeText(part.slice(3,-3))}>کپی کد</button><pre>{part.slice(3,-3)}</pre></div>:part)}</>}
export default function Chat(){const {agentId}=useParams();const agent=agents.find(a=>a.id===agentId);const [wallet,setWallet]=useState<WalletType>(getWallet());const fresh=()=>agent?[{role:"assistant" as const,text:agent.welcome,time:"همین حالا"}]:[];const [messages,setMessages]=useState<Message[]>(fresh);const [input,setInput]=useState("");const [loading,setLoading]=useState(false);const [error,setError]=useState("");const [modal,setModal]=useState(false);if(!agent)return <main className="section text-center"><h1 className="text-3xl font-black">ایجنت پیدا نشد</h1></main>;const estimate=(s:string)=>Math.max(80,Math.ceil(s.length/2)+messages.slice(-10).reduce((n,m)=>n+m.text.length/2,0)+120);function submit(e?:FormEvent){e?.preventDefault();if(!input.trim()||loading)return;const need=estimate(input);const result=canSend(need);if(!result.ok){setError(result.message);setModal(true);return}const now=new Date().toLocaleTimeString("fa-IR",{hour:"2-digit",minute:"2-digit"});setMessages(m=>[...m,{role:"user",text:input,time:now}]);setInput("");setLoading(true);setError("");setTimeout(()=>{const used=Math.min(need,256);setMessages(m=>[...m,{role:"assistant",text:"این پاسخ در حالت نمایشی تولید شده است. در اتصال واقعی، متن به‌صورت استریم از Edge Function دریافت می‌شود.\n\n```ts\nawait chat.stream(message)\n```",time:now,tokens:used}]);setWallet(recordUsage(used));setLoading(false)},600)}function download(){const data=messages.map(m=>`[${m.time}] ${m.role==="user"?"شما":agent!.name}: ${m.text}`).join("\n\n");const u=URL.createObjectURL(new Blob([data],{type:"text/plain;charset=utf-8"}));const a=document.createElement("a");a.href=u;a.download=`گفتگو-${agent!.slug}.txt`;a.click();URL.revokeObjectURL(u)}const percentage=wallet.tokenBalance/wallet.monthlyTokenLimit;const used=messages.reduce((n,m)=>n+(m.tokens||0),0);return <main className="mx-auto flex h-[calc(100vh-73px)] max-w-7xl gap-4 p-4"><aside className="hidden w-70 shrink-0 rounded-2xl border border-white/8 bg-white/[.03] p-4 lg:block"><button className="btn w-full justify-center text-sm" onClick={()=>setMessages(fresh())}>+ گفت‌وگوی جدید</button><p className="mt-7 text-xs text-slate-500">گفتگوهای این ایجنت</p><div className="mt-3 rounded-xl bg-white/7 p-3 text-sm">گفتگوی جدید<br/><span className="text-xs text-slate-500">همین حالا · {fa(used)} توکن</span></div></aside><section className="flex min-w-0 flex-1 flex-col rounded-2xl border border-white/8 bg-[#101936]"><header className="flex items-center justify-between border-b border-white/8 px-5 py-4"><div className="flex gap-3"><span className="text-2xl">{agent.icon}</span><div><b>{agent.name}</b><small className="block text-emerald-400">● آنلاین</small></div></div><div className="flex gap-2"><button className="icon-btn" onClick={download} aria-label="دانلود گفتگو"><Download size={18}/></button><button className="icon-btn" onClick={()=>setMessages(fresh())} aria-label="پاک کردن گفتگو"><Trash2 size={18}/></button></div></header>{percentage<.2&&<div className={`mx-5 mt-4 rounded-xl p-3 text-sm ${percentage<.1?"bg-red-500/15 text-red-100":"bg-amber-400/10 text-amber-100"}`}>موجودی توکن شما رو به اتمام است. برای جلوگیری از توقف گفتگو، اعتبار خود را افزایش دهید.</div>}<div className="flex-1 space-y-5 overflow-y-auto p-5">{messages.map((m,i)=><div className={`flex ${m.role==="user"?"justify-start":"justify-end"}`} key={i}><div className={`max-w-[85%] whitespace-pre-wrap rounded-2xl px-4 py-3 text-sm leading-7 ${m.role==="user"?"bg-violet-600":"border border-white/8 bg-white/[.04]"}`}><Code text={m.text}/><small className="mt-2 block text-slate-500">{m.tokens?`مصرف: ${fa(m.tokens)} توکن · `:""}{m.time}</small></div></div>)}{messages.length===1&&<div className="flex flex-wrap gap-2">{agent.prompts.map(p=><button className="prompt" key={p} onClick={()=>setInput(p)}>{p}</button>)}</div>}{loading&&<div className="typing"><i/><i/><i/></div>}{error&&!modal&&<p className="text-sm text-red-200">{error}</p>}</div><form onSubmit={submit} className="border-t border-white/8 p-4"><div className="flex items-end gap-3 rounded-2xl border border-white/10 bg-[#0b1124] p-2"><button type="button" disabled className="icon-btn opacity-35" aria-label="پیوست غیرفعال"><Paperclip size={18}/></button><textarea className="min-h-11 flex-1 resize-none bg-transparent px-3 py-2 text-sm outline-none" value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();submit()}}} placeholder="پیامت را بنویس…"/><button className="icon-btn bg-violet-600 text-white"><Send size={18}/></button></div></form></section><aside className="hidden w-62 shrink-0 rounded-2xl border border-white/8 bg-white/[.03] p-5 xl:block"><span className="text-4xl">{agent.icon}</span><h2 className="mt-3 font-bold">{agent.name}</h2><p className="mt-1 text-sm text-slate-500">{agent.category}</p><div className="mt-8 border-y border-white/8 py-5"><p className="text-xs text-slate-500">مصرف این گفتگو</p><b className="mt-1 block">{fa(used)} توکن</b><p className="mt-4 text-xs text-slate-500">موجودی توکن شما</p><b className="mt-1 block text-xl">{fa(wallet.tokenBalance)}</b></div><button className="btn btn-soft mt-5 w-full justify-center text-xs" onClick={()=>setModal(true)}><Wallet size={15}/> خرید توکن بیشتر</button><Link className="btn btn-soft mt-3 w-full justify-center text-xs" to="/pricing">ارتقا پلن</Link></aside>{modal&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/75 p-4"><div className="w-full max-w-md rounded-3xl border border-white/10 bg-[#101936] p-6"><button onClick={()=>setModal(false)} className="float-left text-slate-400"><X/></button><h2 className="text-xl font-bold">خرید توکن</h2><p className="mt-3 text-sm leading-7 text-slate-400">{error||"بسته‌ی موردنظر را انتخاب کنید؛ پرداخت در MVP با موفقیت شبیه‌سازی می‌شود."}</p><div className="mt-6 grid gap-3">{[[100000,"۵۰٬۰۰۰ تومان"],[300000,"۱۳۰٬۰۰۰ تومان"],[1000000,"۴۰۰٬۰۰۰ تومان"]].map(([tokens,price])=><button key={String(tokens)} onClick={()=>{setWallet(topUp(Number(tokens)));setError("");setModal(false)}} className="flex justify-between rounded-xl border border-white/10 p-4 hover:border-violet-400"><b>{fa(Number(tokens))} توکن</b><span className="text-violet-300">{price}</span></button>)}</div></div></div>}</main>}
