node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed workspace state, not project source. In the
# client-initiated single-repo workspace layout the repo root IS the
# workspace root, so these land inside this repo rather than beside it.
/.plugins/
/.opencode-skills/
